services:
  ai-app:
    build: .
    volumes:
      - ./:/app
      - venv:/app/.venv  # keeps the container's .venv intact
    ports:
      - 8501:8501
    environment:
      - REMOTE_BASE_URL
      - REMOTE_MODEL_NAME
      - OPENROUTER_API_KEY
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
    depends_on:
      - llm
  llm:
    provider:
      type: model
      options:
        model: ai/qwen2.5

volumes:
  venv: